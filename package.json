{
  "name": "allumino-trajectory-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Allumino MVP server — serves the prototype UI and proxies trajectory generation to the Claude API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
